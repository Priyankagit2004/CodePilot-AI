import { AlertTriangle, CheckCircle2, Info } from 'lucide-react'
import { Card } from '../ui/Card'
import type { Insight, TimelineEvent } from '../../lib/api'
const icon = (severity: string) => severity === 'critical' ? <AlertTriangle className="size-4 text-red-400" /> : severity === 'warning' ? <AlertTriangle className="size-4 text-amber-400" /> : <Info className="size-4 text-blue-400" />
export function Insights({ insights }: { insights: Insight[] }) { return <Card className="p-5"><h2 className="font-medium">AI insights</h2><div className="mt-4 space-y-3">{insights.map((insight) => <div key={insight.title} className="flex gap-3 rounded-md bg-[#0d1117] p-3">{icon(insight.severity)}<div><h3 className="text-sm font-medium">{insight.title}</h3><p className="mt-1 text-xs leading-5 text-slate-400">{insight.detail}</p></div></div>)}</div></Card> }
export function Timeline({ events }: { events: TimelineEvent[] }) { return <Card className="p-5"><h2 className="font-medium">Repository timeline</h2><div className="mt-4 space-y-4">{events.map((event) => <div key={`${event.title}-${event.timestamp}`} className="relative pl-6 before:absolute before:left-1 before:top-1.5 before:size-2 before:rounded-full before:bg-emerald-400"><p className="text-sm font-medium">{event.title}</p><p className="mt-1 text-xs text-slate-400">{event.detail}</p><p className="mt-1 text-[11px] text-slate-600">{new Date(event.timestamp).toLocaleString()}</p></div>)}</div></Card> }
