import { Boxes, GitFork } from 'lucide-react'
import { Card } from '../ui/Card'
import type { ArchitectureDiagram, GraphEdge } from '../../lib/api'
export function DependencyGraph({ edges }: { edges: GraphEdge[] }) { return <Card className="p-5"><div className="flex items-center gap-2"><GitFork className="size-4 text-blue-400" /><h2 className="font-medium">Dependency graph</h2></div><div className="mt-4 max-h-60 space-y-2 overflow-auto">{edges.length ? edges.slice(0, 12).map((edge, index) => <div key={`${edge.source}-${edge.target}-${index}`} className="flex items-center gap-2 rounded bg-[#0d1117] p-2 text-xs"><span className="truncate text-slate-300">{edge.source}</span><span className="text-slate-600">→</span><span className="truncate text-blue-300">{edge.target}</span></div>) : <p className="text-sm text-slate-500">No import dependencies detected.</p>}</div></Card> }
export function ArchitectureGraph({ diagram }: { diagram: ArchitectureDiagram }) { return <Card className="p-5"><div className="flex items-center gap-2"><Boxes className="size-4 text-violet-400" /><h2 className="font-medium">Architecture diagram</h2></div><div className="mt-4 flex flex-wrap gap-2">{diagram.nodes.map((node) => <div key={node.id} className="rounded-md border border-violet-500/30 bg-violet-500/10 px-3 py-2 text-sm text-violet-100"><span className="block text-[10px] uppercase tracking-wide text-violet-300">{node.kind}</span>{node.label}</div>)}</div><p className="mt-4 text-xs text-slate-500">{diagram.edges.length} inferred relationships across the repository.</p></Card> }
